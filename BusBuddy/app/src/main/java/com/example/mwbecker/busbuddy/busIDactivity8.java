package com.example.mwbecker.busbuddy;

import android.graphics.Color;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;

public class busIDactivity8 extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_bus_idactivity8);
        Boolean isOnePressed = false, isSecondPlace = false;
        final Button yES = (Button) findViewById(R.id.yES);
        final Button nO = (Button) findViewById(R.id.nO);

    }
}
